import React from 'react';

const Dashboard: React.FC = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Welcome to Starlink Partner Dashboard</h1>
      <p>Select a section from the sidebar to manage Starlink customer accounts and devices.</p>
    </div>
  );
};

export default Dashboard;
